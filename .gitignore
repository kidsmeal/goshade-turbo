.godot/

# ClauDHD local session state
.now/
.gantry/

# Local Claude Code permissions (machine paths)
.claude/settings.local.json

# Local browser-pane launch config (machine paths)
.claude/launch.json

# Godot editor data dirs, created beside project.godot when the editor runs with the project as its data path
export_templates/
feature_profiles/
script_templates/
text_editor_themes/

# Render output of tests/run_render_checks.gd -- --write-screenshots
sandbox/screenshots/

# Device check output (sandbox/mobile/build_mobile_shaders.gd) and local export preset
sandbox/mobile/shaders/
sandbox/mobile/shader_list.gd
sandbox/mobile/shader_list.gd.uid
export_presets.cfg
